# this db will be created "on-the-fly" by mongodb, so no need to create it first
db.name=evaluation

# the server where mongodb is running
db.server=localhost

# the port where mongodb is running
db.server.port=27017
